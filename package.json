{
  "name": "roleradar",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "worker",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n worker,frontend,scheduler -c blue,magenta,yellow \"npm run dev -w worker\" \"npm run dev -w frontend\" \"node worker/scripts/dev-scheduler.mjs\"",
    "build": "npm run build -w frontend",
    "deploy:worker": "npm run deploy -w worker",
    "typecheck": "npm run typecheck -w worker && npm run typecheck -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9"
  }
}
